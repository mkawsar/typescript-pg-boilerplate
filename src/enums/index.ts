//USER ACCOUNT STATUS ENUMS
export enum Status {
    ACTIVE = 'active',
    INACTIVE = 'inactive'
}

// OTP type enums
export enum OtpType {
    FORGET = 'forget',
    VERIFICATION = 'verification'
}

// User Role
export enum Role {
    USER = 'user',
    ADMIN = 'admin',
    MANAGER = 'manager',
}
